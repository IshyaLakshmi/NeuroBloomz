# Copy this file to .env and fill in values
# SMTP settings (use your SMTP provider / relay credentials)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false # true for TLS/SSL (usually port 465)
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Optional: override From address (defaults to SMTP_USER)
FROM_EMAIL=webform@yourdomain.com

# Optional: override recipients (comma-separated)
TO_EMAILS=ishyadinesh@outlook.com,neurobloomz@outlook.com

# Server port
PORT=3001
